# Copy to .env.local. Only NEXT_PUBLIC_* variables are exposed to the browser.
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Currency used to format amounts (ISO 4217 code)
NEXT_PUBLIC_CURRENCY=INR
